# white-space

## 介绍

`white-space` 设置如何处理元素中的空白。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/white-space/App.tsx"
  img="/assets/doc/css/white-space.jpg"
  entry="src/white-space"
  defaultEntryFile="dist/white-space.lynx.bundle"
/>

## 语法

```css
white-space: normal;

white-space: nowrap;
```

### 取值

- `normal`
  **默认值。**
  不对文本做额外处理。

- `nowrap`
  文本内的换行无效。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>normal</>}
  appliesTo={<>text</>}
  inherited="no"
  animatable="no"
/>

## 形式语法

```
normal | nowrap
```

## 与 Web 的区别

- 只支持 `normal` 和 `nowrap` 两个关键字，和 Web 表现不同。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/white-space" />
