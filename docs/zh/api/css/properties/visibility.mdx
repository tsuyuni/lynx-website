# visibility

## 介绍

CSS 属性 visibility 显示或隐藏元素而不更改文档的布局。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/visibility/App.tsx"
  img="/assets/doc/css/visibility.jpg"
  entry="src/visibility"
  defaultEntryFile="dist/visibility.lynx.bundle"
/>

## 语法

```css
/* Keyword values */
visibility: visible;
visibility: hidden;
```

### 取值

hidden,visible

- **visible**
  默认值，元素正常显示。

- hidden
  隐藏元素，但是其他元素的布局不改变，同时会隐藏其所有孩子，跟 `opacity:0` 效果类似。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>visible</>}
  appliesTo={<>all elements</>}
  inherited="no"
  animatable="yes"
/>

## 形式语法

```
visibility =
  visible   |
  hidden
```

## 与 Web 的区别

不支持 `collapse`、`inherit`、`initial` 和 `unset`
如果设置为 hidden，但其子元素设为 visibility: visible，则该子元素依然不可见

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/visibility" />
