# column-gap (grid-column-gap)

## 介绍

`column-gap`/`grid-column-gap` 属性用来设置[弹性布局](guide/ui/layout/flexible-box-layout.mdx)和[网格布局](guide/ui/layout/grid-layout.mdx)元素列之间的间隙（gutter）大小。

为了兼容性，可使用 `column-gap` 或 `grid-column-gap`。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/column-gap/App.tsx"
  img="/assets/doc/css/column-gap.png"
  defaultEntryFile="dist/column-gap.lynx.bundle"
  entry="src/column-gap"
/>

## 语法

```css
/* <length> values */
column-gap: 20px;
column-gap: 1em;
grid-column-gap: 20px;
grid-column-gap: 1em;

/* <percentage> value */
column-gap: 10%;
grid-column-gap: 10%;

/* calc() values */
column-gap: calc(10% + 20px);
grid-column-gap: calc(10% + 20px);
```

### 取值

- `<length>`

[弹性布局](guide/ui/layout/flexible-box-layout.mdx)或[网格布局](guide/ui/layout/grid-layout.mdx)列之间的间隙宽度。

- `<percentage>`

列之间直接的间隙宽度，相对容器自身 content area **横向大小**的百分比。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      <code>0</code>
    </>
  }
  appliesTo={<>弹性容器和网格容器</>}
  inherited="否"
  animatable="否"
  percentages={<>refer to row dimension of its own content area</>}
/>

## 形式语法

```
column-gap = <length-percentage [0,∞]>

<length-percentage> =
  <length>      |
  <percentage>
```

## 与 Web 的区别

- 不支持 `normal`。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/column-gap" />
