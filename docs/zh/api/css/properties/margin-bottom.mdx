# margin-bottom

## 介绍

`margin-bottom` 属性用于设置元件的底部外边距，允许设置负数值。一个正数值将让它相对于正常流与邻近块更远，而负数值将使得更近。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/margin-bottom/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/margin-bottom.jpg"
  entry="src/margin-bottom"
  defaultEntryFile="dist/margin-bottom.lynx.bundle"
/>

## 语法

```css
/* <length> values */
margin-bottom: 50px;
margin-bottom: 0;
margin-bottom: 2rpx;

/* <percentage> value */
margin-bottom: 10%;
```

### 取值

#### `0px`

**默认值**。

#### `<length>`

[`<length>`](api/css/data-type/length.mdx) 定义了一个确定的宽度值。可以为负值。

#### `<percentage>`

相对于[包含块](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Containing_block)的宽度，以百分比值为外边距。

#### `auto`

让布局流程自己选择一个合适的外边距。

:::info
目前支持在[弹性布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_flexible_box_layout)、[网格布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_grid_layout)、[线性布局](guide/ui/layout/linear-layout.mdx)的非 [`position: fixed/absolute/sticky`](./position) 子元件中使用。

其中，对于线性布局，仅支持在交叉轴方向上使用 `auto`。
:::

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0</>}
  appliesTo={<>所有元件</>}
  inherited="否"
  animatable="yes"
  percentages={<>refer to the width of the containing block</>}
/>

## 形式语法

```
margin-bottom =
  <length-percentage>  |
  auto                 |

<length-percentage> =
  <length>      |
  <percentage>
```

## 与 Web 的区别

[mdn: margin-bottom](https://developer.mozilla.org/zh-CN/docs/Web/CSS/margin-bottom)

1. 在 Lynx 下，`margin` 任何情况都不会重叠。
2. 目前支持在[弹性布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_flexible_box_layout)、[网格布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_grid_layout)、[线性布局](guide/ui/layout/linear-layout.mdx)的非 [`position: fixed/absolute/sticky`](./position) 子元件中使用 `auto`。其中，对于线性布局，仅支持在交叉轴方向上的外边距 `auto` 值。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/margin-bottom" />
