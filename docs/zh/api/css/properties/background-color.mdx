# background-color

## 介绍

`background-color` 会设置元件的背景色，属性的值为颜色 [`<color>`](api/css/data-type/color.mdx)。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/background-color/App.tsx"
  img="/assets/doc/css/background-color.jpg"
  defaultEntryFile="dist/background-color.lynx.bundle"
  entry="src/background-color"
/>

## 语法

```css
/* Keyword values */
background-color: red;

/* Hexadecimal value */
background-color: #bbff00;

/* RGB value */
background-color: rgb(255, 255, 128);

/* HSLA value */
background-color: hsla(50, 33%, 25%, 0.75);
```

### 取值

- [`<color>`](api/css/data-type/color.mdx) <br/>
  描述背景颜色。该属性只能指定一个颜色值。

- 默认值 **transparent** <br />

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>transparent</>}
  appliesTo={<>all elements</>}
  inherited="no"
  animatable="yes"
/>

## 形式语法

```
<bg-color> = <rgb()> | <rgba()> | <hsl()> | <hsla()> | <hex-color> | <named-color>
```

## 与 Web 的区别

- 不支持特殊关键字，例如 `currentcolor`
- 不支持全局关键字，例如 `inherit`、`initial`

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/background-color" />
