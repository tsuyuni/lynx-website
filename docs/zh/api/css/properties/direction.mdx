import { Deprecated } from '@lynx';

# direction

## 介绍

`direction` 属性设置文字、元素的布局方向。例如 `ltr` 表示从左向右 (类似中文等)，`rtl` 表示从右向左 (类似阿拉伯语等)。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  entry="src/direction"
  defaultFile="src/direction/App.tsx"
  img="/assets/doc/css/direction.jpg"
  defaultEntryFile="dist/direction.lynx.bundle"
  entry="src/direction"
/>

## 语法

```css
direction: normal;
direction: ltr;
direction: rtl;
direction: lynx-rtl;
```

### 取值

- `normal`

  **默认值**。与 `ltr` 表现一致，是兼容产物。

- `ltr`

  文字从左向右排列，水平方向的逻辑属性 `start` 映射到 `left`，`end` 映射到 `right`。

- `rtl`

  文字从右向左排列，水平方向的逻辑属性 `start` 映射到 `right`，`end` 映射到 `left`。

- `lynx-rtl` <Deprecated/>

  **lynx 兼容属性**，文字从右向左排列，水平方向的属性 `left` 映射到 `right`，`right` 映射到 `left`。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>normal</>}
  appliesTo={<>所有元件</>}
  inherited="是"
/>

## 形式语法

```
direction = normal | ltr | rtl
```

## 与 Web 的区别

- 引入 `normal`、`lynx-rtl` 等兼容属性，用来提高 lynx 易用性。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/direction" />
