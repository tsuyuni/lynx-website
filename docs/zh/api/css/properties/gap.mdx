# gap

## 介绍

`gap` 属性是用来设置[弹性布局](guide/ui/layout/flexible-box-layout.mdx)和[网格布局](guide/ui/layout/grid-layout.mdx)行与列之间的间隙（gutters），该属性是 [`row-gap`](./row-gap.mdx) 和 [`column-gap`](./column-gap.mdx) 的简写形式（`column-gap` 是可选的）。

假如 `column-gap` 缺失的话，则会被设置成跟 `row-gap` 一样的的值。`row-gap` 和 `column-gap` 都可以用 [`<length>`](api/css/data-type/length.mdx) 或者 [`<percentage>`](api/css/data-type/percentage.mdx) 来表示。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/gap/App.tsx"
  img="/assets/doc/css/gap.png"
  defaultEntryFile="dist/gap.lynx.bundle"
  entry="src/gap"
/>

## 语法

```css
/* One <length> value */
gap: 20px;
gap: 1em;

/* One <percentage> value */
gap: 16%;
gap: 100%;

/* Two <length> values */
gap: 20px 10px;
gap: 1em 0.5em;

/* One or two <percentage> values */
gap: 16% 100%;
gap: 21px 82%;

/* calc() values */
gap: calc(10% + 20px);
gap: calc(20px + 10%) calc(10% - 5px);
```

### 取值

- `<length>`

  [弹性布局](guide/ui/layout/flexible-box-layout.mdx)或[网格布局](guide/ui/layout/grid-layout.mdx)行与列之间的间隙宽度。

- `<percentage>`

  行与列之间直接的间隙宽度，相对容器自身 content area 对应轴尺寸的百分比。比如说，[`row-gap`](./row-gap.mdx) 使用 `percentage` 时，是相对于自身 `content area` **竖向大小**的百分比。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      as each of the properties of the shorthand: <br />
      <ul style={{ listStyleType: 'disc', paddingLeft: '1.5rem' }}>
        <li>
          <code>row-gap: 0</code>
        </li>
        <li>
          <code>column-gap: 0</code>
        </li>
      </ul>
    </>
  }
  appliesTo={<>弹性容器和网格容器</>}
  inherited="否"
  percentages={
    <>
      refer to corresponding dimension of the content area(e.g.,
      <code>row-gap</code> refer to column axis size)
    </>
  }
/>

## 形式语法

```
gap =
  <'row-gap'> <'column-gap'>?

<row-gap> = <length-percentage [0,∞]>

<column-gap> = <length-percentage [0,∞]>

<length-percentage> =
  <length>      |
  <percentage>
```

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/gap" />
