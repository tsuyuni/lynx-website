# border-inline-end-color

## 介绍

用于设置阅读模式结束方向的边框颜色。

在 `LTR` 模式下，`border-inline-end-color` 等价于 `border-right-color`。
在 `RTL` 模式下，`border-inline-end-color` 等价于 `border-left-color`。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/border-inline-end-color/App.tsx"
  img="/assets/doc/css/border-inline-color.jpg"
  defaultEntryFile="dist/border-inline-end-color.lynx.bundle"
  entry="src/border-inline-end-color"
/>

## 语法

```css
border-inline-end-color: red;
border-inline-end-color: #ffbb00;
border-inline-end-color: rgb(255, 0, 0);
border-inline-end-color: hsla(100%, 50%, 25%, 0.75);
border-inline-end-color: currentColor;
border-inline-end-color: transparent;
```

### 取值

- `transparent`
  边框颜色为透明。

- [`<color>`](/api/css/data-type/color)
  边框具体的颜色。

- 默认值
  **block**

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0px</>}
  appliesTo={<>all elements</>}
  inherited="no"
  animatable="no"
/>

## 形式语法

```
transparent | <color>
```

## 与 Web 的区别

- [mdn:border-inline-end-color](https://developer.mozilla.org/en-US/docs/Web/CSS/border-inline-end-color)

## 扩展阅读

- [direction](/api/css/properties/direction)

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/border-inline-end-color" />
