import { Go } from '@lynx';

# transition

## 介绍

用于设置元件过渡动画效果，是 `transition-property`、`transition-duration`、`transition-timing-function`、`transition-delay` 的简写属性。

## 使用示例

<Go
  example="css-api"
  img="/assets/doc/css/transition.gif"
  defaultEntryFile="dist/transition.lynx.bundle"
  entry="src/transition"
  defaultFile="src/transition/App.tsx"
  entry="src/transition"
/>

## 语法

```css
/* Apply to 1 property */
/* property name | duration */
transition: background-color 4s;

/* property name | duration | delay */
transition: opacity 4s 1s;

/* property name | duration | easing function */
transition: background-color 4s ease-in-out;

/* property name | duration | easing function | delay */
transition: background-color 4s ease-in-out 1s;

/* Apply to 2 properties */
transition:
  background-color 4s,
  opacity 4s;

/* Apply to all changed properties */
transition: all 0.5s ease-out;

/* Clear all transition animation.*/
transition: none;
```

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>空值</>}
  appliesTo={<>所有元件</>}
  inherited="no"
  animatable="no"
/>

## 形式语法

```css
transition: <transition-property> || <transition-duration> ||
  <transition-timing-function> || <transition-delay>;
```

## 动画事件

- [动画事件](../../lynx-api/event/animation-event.mdx)

## 扩展阅读

- [transition-property](./transition-property.mdx)
- [transition-duration](./transition-duration.mdx)
- [transition-delay](./transition-delay.mdx)
- [transition-timing-function](./transition-timing-function.mdx)
- [CSS animation event](../../lynx-api/event/animation-event.mdx)

## 注意事项

:::info

- 目前仅支持 [transition-property](./transition-property.mdx) 中的属性。

:::

:::info

由于 reactLynx 的属性提取问题，写在 Constructor 里的属性不会被提取到首屏，而是会通过数据回设更新，因此可能会触发预期之外的动画

:::

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/transition" />
