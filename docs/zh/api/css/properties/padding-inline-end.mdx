# padding-inline-end

## 介绍

定义了元件的逻辑行末内边距，并根据元件的 [`direction`](./direction) 对应至实体内边距。该属性不能为负值。

在 `LTR` 模式下，`padding-inline-end` 等价于 [`padding-right`](./padding-right)。
在 `RTL` 模式下，`padding-inline-end` 等价于 [`padding-left`](./padding-left)。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/padding-inline-end/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/padding-inline.jpg"
  entry="src/padding-inline-end"
  defaultEntryFile="dist/padding-inline-end.lynx.bundle"
/>

## 语法

```css
/* <length> values */
padding-inline-end: 0.5em;
padding-inline-end: 0px;
padding-inline-end: 2rpx;

/* <percentage> value */
padding-inline-end: 10%;
```

### 取值

#### `0px`

**默认值**。

#### `<length>`

[`<length>`](api/css/data-type/length.mdx) 定义为一个绝对值。不能为负值。

#### `<percentage>`

相对于[包含块](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Containing_block)的行内尺寸（宽度）的百分比。必须是非负值。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0</>}
  appliesTo={<>所有元件</>}
  inherited="否"
  percentages={<>refer to the width of the containing block</>}
/>

## 形式语法

```
padding-inline-end =
  <length-percentage [0,∞]>

<length-percentage> =
  <length>      |
  <percentage>
```

## 与 Web 的区别

[mdn: padding-inline-end](https://developer.mozilla.org/zh-CN/docs/Web/CSS/padding-inline-end)

1. Lynx 暂不支持 `writing mode`。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/padding-inline-end" />
