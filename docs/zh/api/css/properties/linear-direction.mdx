import { Deprecated } from '@lynx';

# linear-direction

## 介绍

`linear-direction` 定义[线性布局](guide/ui/layout/linear-layout.mdx)中的元件排列方向（默认为**垂直**布局），该方向作为线性布局的**主轴**，另外一个方向称为**交叉轴**。

请注意，值 `row`、`row-reverse` 受线性布局中的 [`direction`](./direction.html) 影响。例如，如果 `dir` 属性是 `rtl` 或者 `lynx-rtl`，`row` 表示从右到左定向的轴，而 `row-reverse` 表示从左到右。

:::info
`linear-orientation` 已被废弃，并计划在未来版本中移除。
:::

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/linear-direction/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/linear-direction.png"
  defaultEntryFile="dist/linear-direction.lynx.bundle"
  entry="src/linear-direction"
/>

## 语法

```css
linear-direction: column;

/* Like <column>, but reversed */
linear-direction: column-reverse;

linear-direction: row;

/* Like <row>, but reversed */
linear-direction: row-reverse;
```

### 取值

- `column`

  **默认值**。线性布局中的元件按照垂直方向进行布局。主轴的方向为垂直方向。

- `row`

  线性布局中的元件按照水平方向进行布局。主轴的方向为水平方向。

- `column-reverse`

  和 `column` 表现一致，但是置换了主轴起点和主轴终点。

- `row-reverse`

  和 `row` 的表现一致，但是置换了主轴起点和主轴终点。

- `vertical` <Deprecated/>

  同 `column`。

- `horizontal` <Deprecated/>

  同 `row`。

- `horizontal-reverse` <Deprecated/>

  同 `row-reverse`。

- `vertical-reverse` <Deprecated/>

  同 `column-reverse`。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>column</>}
  appliesTo={<>线性容器</>}
  inherited="否"
/>

## 形式语法

```
linear-direction = column | row | column-reverse | row-reverse
```

## 与 Web 的区别

- Web 无该样式。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/linear-direction" />
