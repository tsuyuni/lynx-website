import { Deprecated } from '@lynx';

# <Deprecated /> linear-cross-gravity

:::warning
该 API 已被废弃。
:::

## 介绍

[线性布局](guide/ui/layout/linear-layout.mdx)中，子元素在垂直于父容器布局方向（交叉轴）上的位置，是父容器上的属性。类似于[弹性布局](guide/ui/layout/flexible-box-layout.mdx)的 [`align-items`](./align-items.mdx)。

当在线性布局中同时使用 [`linear-layout-gravity`](./linear-layout-gravity.mdx)、[`align-self`](./align-self.mdx)、[`linear-cross-gravity`](./linear-cross-gravity.mdx) 和 [`align-items`](./align-items.mdx) 时，优先级从前到后依次降低。优先级高的会覆盖优先级低的样式。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  entry="src/linear-cross-gravity"
  defaultFile="src/linear-cross-gravity/App.tsx"
  img="/assets/doc/css/linear-cross-gravity.png"
  entry="src/linear-cross-gravity"
  defaultEntryFile="dist/linear-cross-gravity.lynx.bundle"
  entry="src/linear-cross-gravity"
/>

## 语法

```css
linear-cross-gravity: none;
linear-cross-gravity: start;
linear-cross-gravity: end;
linear-cross-gravity: center;
linear-cross-gravity: stretch;
```

### 取值

- `none`

  **默认值**。当容器（linear containers）的交叉轴上的大小（如 [`linear-direction: column`](./linear-direction) 时的 [`width`](./width)）固定，且该子元素在该方向的宽度为 `auto` 时，`none` 将等同于 `stretch`。在其他情况下则等同与 `start`。

- `start`

  子元素对齐父容器的交叉轴开始端。

- `end`

  子元素对齐父容器的交叉轴结束端。

- `center`

  子元素在父容器交叉轴居中。

- `stretch`

  子元素填满父容器交叉轴。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>none</>}
  appliesTo={<>线性容器</>}
  inherited="否"
/>

## 形式语法

```
linear-cross-gravity = none | start | end | center | stretch
```

## 与 Web 的区别

- Web 无该样式。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/linear-cross-gravity" />
