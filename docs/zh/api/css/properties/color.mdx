# color

## 介绍

`color` 属性指定文本的颜色，也支持通过 gradient 设置文本渐变色效果。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/color/App.tsx"
  img="/assets/doc/css/color.jpg"
  defaultEntryFile="dist/color.lynx.bundle"
  entry="src/color"
/>

## 语法

```css
/* <named-color> values */
color: red;
color: orange;
color: tan;

/* <hex-color> values */
color: #090;
color: #009900;
color: #090a;
color: #009900aa;

/* <rgb()> values */
color: rgb(34, 12, 64, 0.6);
color: rgba(34, 12, 64, 0.6);
color: rgb(34 12 64 / 0.6);
color: rgba(34 12 64 / 0.3);
color: rgb(34 12 64 / 60%);
color: rgba(34.6 12 64 / 30%);

/* <hsl()> values */
color: hsl(30, 100%, 50%, 0.6);
color: hsla(30, 100%, 50%, 0.6);
color: hsl(30 100% 50% / 0.6);
color: hsla(30 100% 50% / 0.6);
color: hsl(30 100% 50% / 60%);
color: hsla(30.2 100% 50% / 60%);
```

### 取值

- [`<color>`](api/css/data-type/color.mdx)

- [`<gradient>`](api/css/data-type/gradient.mdx)

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>透明色</>}
  appliesTo={<>text</>}
  inherited="no"
  animatable="yes"
/>

## 形式语法

```
<color> | <gradient>
```

## 注意事项

为 `gradient` 类型时，只有 `text` 元件有效。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/color" />
