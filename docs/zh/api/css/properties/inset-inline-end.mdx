# inset-inline-end

## 介绍

CSS 属性 `inset-inline-end` 定义了元件的逻辑行末偏移，并根据元件的行内方向对应至实体偏移。根据 [`direction`](./direction) 所定义的值，此属性对应于 [`right`](./right) 或 [`left`](./left) 属性。

在 `LTR` 模式下，`inset-inline-end` 等价于 `right`。
在 `RTL` 模式下，`inset-inline-end` 等价于 `left`。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/inset-inline-end/App.tsx"
  img="/assets/doc/css/inset-inline-end.jpg"
  entry="src/inset-inline-end"
  defaultEntryFile="dist/inset-inline-end.lynx.bundle"
/>

## 语法

```css
/* <length> values */
inset-inline-end: 3px;
inset-inline-end: 2rpx;
inset-inline-end: 2.4em;
inset-inline-end: 3rem;

/* 参照于包含块宽度的百分比 */
inset-inline-end: 10%;

/* Keyword value */
inset-inline-end: auto;

/* calc */
inset-inline-end: calc(1px + 1px);
```

### 取值

`inset-inline-end` 属性的取值与 [`right`](./right) 属性相同。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>auto</>}
  appliesTo={<>所有元件</>}
  inherited="否"
  percentages={<>refer to the width of the containing block</>}
/>

## 形式语法

与 [`right`](./right) 属性相同。

## 与 Web 的区别

- Lynx 不支持 `writing-mode`；

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/inset-inline-end" />
