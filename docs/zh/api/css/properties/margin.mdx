# margin

## 介绍

`margin` 属性为给定元素设置所有四个（上右下左）方向的外边距属性。也就是 [`margin-top`](./margin-top.mdx)、[`margin-right`](./margin-right.mdx)、[`margin-bottom`](./margin-bottom.mdx)、[`margin-left`](./margin-left.mdx) 四个外边距属性设置的简写。该属性可以为负值。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/margin/App.tsx"
  img="/assets/doc/css/margin.jpg"
  entry="src/margin"
  defaultEntryFile="dist/margin.lynx.bundle"
/>

## 语法

```css
/* Apply to all four sides */
margin: 1em;
margin: -3px;

/* top and bottom | left and right */
margin: 5% auto;

/* top | left and right | bottom */
margin: 1em auto 2em;

/* top | right | bottom | left */
margin: 2px 1em 0 auto;
```

`margin` 属性接受 1~4 个值。每个值可以是 [`length`](api/css/data-type/length.mdx)，[`percentage`](api/css/data-type/percentage.mdx)，或 `auto`。取值为负时元素会比原来更接近临近元素。

当只指定一个值时，该值会统一应用到全部四个边的外边距上。
指定两个值时，第一个值会应用于上边和下边的外边距，第二个值应用于左边和右边。
指定三个值时，第一个值应用于上边，第二个值应用于右边和左边，第三个则应用于下边的外边距。
指定四个值时，依次（顺时针方向）作为上边，右边，下边，和左边的外边距。

### 取值

#### `0px`

**默认值**。

#### `<length>`

[`<length>`](api/css/data-type/length.mdx) 定义了一个确定值。可以为负值。

#### `<percentage>`

相对于[包含块](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Containing_block)的宽度，以百分比值为外边距。

#### `auto`

让布局流程自己选择一个合适的外边距。

:::info
目前支持在[弹性布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_flexible_box_layout)、[网格布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_grid_layout)、[线性布局](guide/ui/layout/linear-layout.mdx)的非 [`position: fixed/absolute/sticky`](./position) 子元素中使用。

其中，对于线性布局，仅支持在交叉轴方向上的外边距 `auto` 值。
:::

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      as each of the properties of the shorthand: <br />
      <ul style={{ listStyleType: 'disc', paddingLeft: '1.5rem' }}>
        <li>margin-bottom: 0</li>
        <li>margin-left: 0</li>
        <li>margin-right: 0</li>
        <li>margin-top: 0</li>
      </ul>
    </>
  }
  appliesTo={<>所有元件</>}
  inherited="否"
  percentages={<>refer to the width of the containing block</>}
/>

## 形式语法

```
margin =
  <length-percentage>  |
  auto                 |

<length-percentage> =
  <length>      |
  <percentage>
```

## 与 Web 的区别

[mdn: margin](https://developer.mozilla.org/zh-CN/docs/Web/CSS/margin)

1. 在 Lynx 下，`margin` 任何情况都不会重叠。
2. 目前支持在[弹性布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_flexible_box_layout)、[网格布局](https://developer.mozilla.org/zh/docs/Web/CSS/CSS_grid_layout)、[线性布局](guide/ui/layout/linear-layout.mdx)的非 [`position: fixed/absolute/sticky`](./position) 子元素中使用 `auto`。其中，对于线性布局，仅支持在交叉轴方向上的外边距 `auto` 值。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/margin" />
