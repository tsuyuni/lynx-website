# border-inline-end-width

## 介绍

用于设置阅读模式结束方向的边框宽度。

在 `LTR` 模式下，`border-inline-end-width` 等价于 `border-right-width`。
在 `RTL` 模式下，`border-inline-end-width` 等价于 `border-left-width`。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/border-inline-end-width/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/border-inline-width.jpg"
  defaultEntryFile="dist/border-inline-end-width.lynx.bundle"
  entry="src/border-inline-end-width"
/>

## 语法

```css
border-inline-end-width: thin;
border-inline-end-width: medium;
border-inline-end-width: thick;
border-inline-end-width: 10px;
```

### 取值

- `thin`
  细边框。

- `medium`
  中等边框。

- `thick`
  粗边框。

- [`<length>`](/api/css/data-type/length)
  以具体的尺寸定义边框宽度

- 默认值
  **0px**

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>solid</>}
  appliesTo={<>all elements</>}
  inherited="no"
  animatable="no"
/>

## 形式语法

```
thin | medium | thick | <length>
```

## 与 Web 的区别

- [mdn:border-inline-end-width](https://developer.mozilla.org/en-US/docs/Web/CSS/border-inline-end-width)

## 扩展阅读

- [direction](/api/css/properties/direction)

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/border-inline-end-width" />
