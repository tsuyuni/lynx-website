# width

## 介绍

指定了一个元件的宽度。
默认情况下，这个属性决定的是边框区域（border area）的宽度，但如果将该元件的 [`box-sizing`](./box-sizing.mdx) 设置为 `content-box`，就转而设置内容区域（content area）的宽度。

[`min-width`](./min-width) 和 [`max-width`](./max-width) 属性的优先级高于 `width`。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/width/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/width.jpg"
  entry="src/width"
  defaultEntryFile="dist/width.lynx.bundle"
/>

## 语法

```css
/* <length> values */
width: 300px;
width: 25em;

/* <percentage> value */
width: 75%;

/* Keyword values */
width: max-content;
width: fit-content;
width: fit-content(20em);
width: auto;
```

### 取值

- `auto`

  **默认值**。`lynx` 根据一些条件决定具体值。

- `<length>`

  [`<length>`](api/css/data-type/length.mdx) 将宽度定义为一个绝对值。

- `<percentage>`

  使用外层元件的容纳区块宽度（the containing block's width）的百分比定义宽度。

- `max-content`

元件内容固有的（intrinsic）合适宽度。

- `fit-content`

  在内容最大宽度及 [`fit-content`](api/css/data-type/fit-content.mdx) 定义值中取最小值。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      <code>auto</code>
    </>
  }
  appliesTo={<>所有元件</>}
  inherited="否"
  animatable="是"
  percentages={<>refer to the width of the containing block</>}
/>

## 与 Web 的区别

[mdn: width](https://developer.mozilla.org/en-US/docs/Web/CSS/width)

- `width` 默认决定的是边框区域（border area）的宽度，而 Web 是 `content area`；
- 不支持 `min-content`；

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/width" />
