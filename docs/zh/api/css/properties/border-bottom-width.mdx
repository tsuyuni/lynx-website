# border-bottom-width

## 介绍

用于设置下边框的宽度。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/border-bottom-width/App.tsx"
  img="/assets/doc/css/border-bottom-width.jpg"
  defaultEntryFile="dist/border-bottom-width.lynx.bundle"
  entry="src/border-bottom-width"
/>

## 语法

```css
border-bottom-width: 10px;
```

### 取值

- `thin`：细边框（2px）
- `medium`：中等边框（4px）
- `thick`：粗边框（6px）
- [`<length>`](api/css/data-type/length.mdx)：具体尺寸值

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0</>}
  appliesTo={<>所有元素</>}
  inherited="no"
  animatable="yes"
/>

## 形式语法

```css
border-bottom-width = <line-width>
<line-width> = thin | medium | thick | <length>

```

## 与 Web 的区别

- [MDN 参考](https://developer.mozilla.org/zh-CN/docs/Web/CSS/border-bottom-width)
- 具体尺寸值不同（Web 标准：`thin=1px, medium=3px, thick=5px`）
- 默认值不同（Web 为 `medium`）
- 不支持 `inherit/initial/revert/unset` 全局值

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/border-bottom-width" />
