# aspect-ratio

## 介绍

设置元素宽高比，即 [`width`](./width) / [`height`](./height)。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/aspect-ratio/App.tsx"
  img="/assets/doc/css/aspect-ratio.jpg"
  defaultEntryFile="dist/aspect-ratio.lynx.bundle"
  entry="src/aspect-ratio"
/>

## 语法

```css
aspect-ratio: 1 / 1;
aspect-ratio: 16 / 9;
aspect-ratio: 0.5;
```

### 取值

#### `<ratio>`

**默认为无宽高比**。盒子的宽高比为指定的 [`width`](./width) / [`height`](./height) 比率。如果省略 `height` 和前面的斜杠字符，则 `height` 默认为 1。涉及宽高比的尺寸计算使用由 [`box-sizing`](./box-sizing) 所指定的盒子的尺寸。

:::info
`<ratio>` CSS 数据类型，表示两个无单位值之间的比例。

句法

`<ratio>` 数据类型包括一个正数的 [`<number>`](api/css/data-type/number.mdx) 值，后跟一个正斜杠（'/'，Unicode U + 002F SOLIDUS）和第二个正数的 `<number>` 值。此外，允许使用单个正数 `<number>` 作为值。
:::

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      has no preferred <code>aspect ratio</code>
    </>
  }
  appliesTo={<>all elements</>}
  inherited="no"
/>

## 形式语法

```
aspect-ratio = <number [0,∞]> [ / <number [0,∞]> ]
```

## 与 Web 的区别

[mdn: aspect-ratio](https://developer.mozilla.org/en-US/docs/Web/CSS/aspect-ratio)

1. 不支持 `auto`，默认为无宽高比。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/aspect-ratio" />
