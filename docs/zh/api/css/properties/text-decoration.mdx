# text-decoration

## 介绍

`text-decoration` 属性用于设置文本的修饰线外观。它是 `text-decoration-line`, `text-decoration-color`, `text-decoration-style` 属性的缩写。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/text-decoration/App.tsx"
  img="/assets/doc/css/text-decoration.jpg"
  entry="src/text-decoration"
  defaultEntryFile="dist/text-decoration.lynx.bundle"
/>

## 语法

```css
text-decoration: underline;
text-decoration: line-through red;
text-decoration: none;
```

### 取值

#### **text-decoration-line**

用于设置元件中的文本的修饰类型。

- `none` <br/>
  **默认值**无修饰线。

- `underline`
  在文本的下方有一条修饰线。

- `line-through`
  有一条贯穿文本中间的修饰线。

#### **text-decoration-color**

- [`<color>`](api/css/data-type/color)
  修饰文本的线的颜色。

#### **text-decoration-style**

用于设置由 `text-decoration-line` 设定的线的样式

- `solid`
  画一条实线。

- `double`
  画一条双实线。

- `dotted`
  画一条点划线。

- `dashed`
  画一条虚线。

- `wavy`
  画一条波浪线。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>空值</>}
  appliesTo={<>text</>}
  inherited="yes"
  animatable="no"
/>

## 与 Web 的区别

- 不支持 [`text-decoration-thickness`](https://developer.mozilla.org/zh-CN/docs/Web/CSS/text-decoration-thickness)
- 不支持在同一个文本上设置多条修饰线。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/text-decoration" />
