# text-align

## 介绍

`text-align` 属性设置文本内容的对齐方式。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/text-align/App.tsx"
  img="/assets/doc/css/text-align.jpg"
  entry="src/text-align"
  defaultEntryFile="dist/text-align.lynx.bundle"
/>

## 语法

```css
/* Keyword values */
text-align: start;
text-align: end;
text-align: left;
text-align: right;
text-align: center;
```

### 取值

- `left`
  行内内容向左侧边对齐。

- `right`
  行内内容向右侧边对齐。

- `center`
  行内内容居中。

- `start`
  文字对齐阅读开始方向侧边。
  `LTR` 模式下等价 `left`；`RTL` 模式下等价 `right`。
  **当 `direction` 为 `normal` 时，`start` 跟随文字内容方向。**

- `end`
  文字对齐阅读开始方向侧边。
  `LTR` 模式下等价 `right`；`RTL` 模式下等价 `left`。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>start</>}
  appliesTo={<>text</>}
  inherited="yes"
  animatable="no"
/>

## 形式语法

```
left | right | center | start | end
```

## 与 Web 的区别

- 不支持 `justify` 和 `justify-all`。

- 不支持 `match-parent`。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/text-align" />
