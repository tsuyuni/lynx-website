# padding

## 介绍

指定一个元素内容与内边界的距离，该属性不能为负值。

是 [`padding-top`](./padding-top.mdx)、[`padding-right`](./padding-right.mdx)、[`padding-bottom`](./padding-bottom.mdx)、[`padding-left`](./padding-left.mdx) 的简写。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/padding/App.tsx"
  img="/assets/doc/css/padding.jpg"
  entry="src/padding"
  defaultEntryFile="dist/padding.lynx.bundle"
/>

## 语法

```css
/* Apply to all four sides */
padding: 1em;

/* top and bottom | left and right */
padding: 5% 10%;

/* top | left and right | bottom */
padding: 1em 2em 2em;

/* top | right | bottom | left */
padding: 5px 1em 0 2em;
```

`padding` 属性接受 1~4 个值。每个值可以是 [`<length>`](api/css/data-type/length.mdx) 或 [`<percentage>`](api/css/data-type/percentage.mdx)。取值不能为负。

当只指定一个值时，该值会统一应用到全部四个边的内边距上。
指定两个值时，第一个值会应用于上边和下边的内边距，第二个值应用于左边和右边。
指定三个值时，第一个值应用于上边，第二个值应用于右边和左边，第三个则应用于下边的内边距。
指定四个值时，依次（顺时针方向）作为上边，右边，下边，和左边的内边距。

### 取值

#### `0px`

**默认值**。

#### `<length>`

[`<length>`](api/css/data-type/length.mdx) 定义为一个绝对值。不能为负值。

#### `<percentage>`

相对于[包含块](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Containing_block)的行内尺寸（宽度）的百分比。必须是非负值。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      as each of the properties of the shorthand: <br />
      <ul style={{ listStyleType: 'disc', paddingLeft: '1.5rem' }}>
        <li>padding-bottom: 0</li>
        <li>padding-left: 0</li>
        <li>padding-right: 0</li>
        <li>padding-top: 0</li>
      </ul>
    </>
  }
  appliesTo={<>所有元件</>}
  inherited="否"
  percentages={<>refer to the width of the containing block</>}
/>

## 形式语法

```
padding =
  <'padding-top'>{1,4}

<padding-top> =
  <length-percentage [0,∞]>

<length-percentage> =
  <length>      |
  <percentage>
```

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/padding" />
