# relative-align-top

## 介绍

在[相对布局](guide/ui/layout/relative-layout.mdx)中，指定当前元素与父亲或者对应 [`id`](./relative-id.mdx) 的同级元素的上边缘对齐。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  entry="src/relative-align-top"
  defaultFile="src/relative-align-top/App.tsx"
  img="/assets/doc/css/relative-align-top.png"
  defaultEntryFile="dist/relative-align-top.lynx.bundle"
  entry="src/relative-align-top"
/>

## 语法

```css
relative-align-top: parent;
relative-align-top: none;

relative-align-top: 1;
relative-align-top: 2;
```

### 取值

- `none`

  **默认值**。不在上边缘对齐。

- `parent`

  上边缘与父元素对齐。

- `<number>`

  `int` 值。上边缘与此值（参考 [`relative-id`](./relative-id.mdx)）所对应的同级元素对齐。

  :::info
  [`id`](./relative-id.mdx) 的设定请注意：`-1` 和 `0` 是特殊值。`-1` 是默认值，代表当前元素不会被视为同级元素的对齐参考标准。不能使用 `0` 作为元素的 id，否则有不符合预期的表现。
  :::

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      <code>none</code>
    </>
  }
  appliesTo={<>relative items</>}
  inherited="否"
/>

## 形式语法

```
relative-align-top = none | parent | <number>
```

## 与 Web 的区别

- Web 无此属性。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/relative-align-top" />
