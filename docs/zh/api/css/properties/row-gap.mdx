# row-gap (grid-row-gap)

## 介绍

`row-gap`/`grid-row-gap` 属性用来设置[弹性布局](guide/ui/layout/flexible-box-layout.mdx)和[网格布局](guide/ui/layout/grid-layout.mdx)行元件之间的间隙（gutter）大小。

为了兼容性，可使用 `row-gap` 或 `grid-row-gap`。

## 使用示例

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/row-gap/App.tsx"
  img="/assets/doc/css/row-gap.png"
  defaultEntryFile="dist/row-gap.lynx.bundle"
  entry="src/row-gap"
/>

## 取值

### `<length>`

[`<length>`](api/css/data-type/length.mdx) 表示[弹性布局](guide/ui/layout/flexible-box-layout.mdx)或[网格布局](guide/ui/layout/grid-layout.mdx)行之间的间隙宽度。

### `<percentage>`

行之间直接的间隙宽度，[`<percentage>`](api/css/data-type/percentage.mdx) 相对容器自身 content area **纵向**大小的百分比。

## 形式语法

## 语法

```css
/* <length> values */
row-gap: 20px;
grid-row-gap: 20px;
row-gap: 1em;
grid-row-gap: 1em;

/* <percentage> value */
row-gap: 10%;
grid-row-gap: 10%;

/* calc() values */
row-gap: calc(10% + 20px);
grid-row-gap: calc(10% + 20px);
```

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      <code>0</code>
    </>
  }
  appliesTo={<>弹性容器和网格容器</>}
  inherited="否"
  percentages={<>refer to vertical dimension of its own content area</>}
/>

## 与 Web 的区别

[mdn: row-gap](https://developer.mozilla.org/en-US/docs/Web/CSS/row-gap)

1. 不支持 `normal`。

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/row-gap" />

## 常见问题
