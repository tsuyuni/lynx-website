# findUIByIdSelector

根据 `idSelector` 属性查找 `LynxUI` 节点；

## 语法

### Android

```java
public LynxBaseUI findUIByIdSelector(String id);
```

### 参数说明

- `id: String`：前端在布局文件中指定的视图的 id 属性；

### iOS

```objc
- (nullable LynxUI*)uiWithIdSelector:(nonnull NSString*)idSelector;
```

### 参数说明

- `idSelector: NSString*`：前端在布局文件中指定的视图的 id 属性；

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="lynx-native-api/lynx-view/find-ui-by-idselector" />
