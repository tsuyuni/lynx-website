# destroy

在 `LynxView` 不可用之后，需要主动调用该方法释放 `LynxView` 内存，否则会造成内存泄露；

## 语法

### Android

```java
public void destroy();
```

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="lynx-native-api/lynx-view/destroy" />
