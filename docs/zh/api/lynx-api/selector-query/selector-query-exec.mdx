---
title: exec()
---

import { Tab, Tabs } from 'rspress/theme';
import { RuntimeBadge } from '@lynx';

# SelectorQuery: exec() method

<RuntimeBadge type={'bts'} />

执行所有已提交的操作。注意不调用 `exec()` 则节点查找和节点操作都不会实际执行。

## 语法

```ts
exec(): void;
```

### 参数

无。

### 返回值

无（`undefined`）。

## 示例

```ts
lynx
  .createSelectorQuery()
  .select('#my-id')
  .invoke({
    method: 'boundingClientRect',
    success: function (res) {
      console.log(res);
    },
    fail: function (res) {
      console.log(res.code, res.data);
    },
  })
  .exec();
```

## 兼容性

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="lynx-api/selector-query/exec" />
