# `<max-content>`

`max-content` currently only supports for width and height. `max-content` is the maximum content size in the relevant axis; .

## Example

```css
width: max-content;
```

## Compatibility

import { APITable } from '@lynx';

<APITable query="css/data-type/max-content" />
