---
id: flex-wrap
slug: flex-wrap
title: flex-wrap
---

## Introduction

The `flex-wrap` CSS property sets whether flex items are forced onto one line or can wrap onto multiple lines. If wrapping is allowed, it sets the direction that lines are stacked.

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/flex-wrap/App.tsx"
  img="/assets/doc/css/flex-wrap.jpg"
  entry="src/flex-wrap"
  defaultEntryFile="dist/flex-wrap.lynx.bundle"
/>

## Syntax

```css
flex-wrap: nowrap; /* Default value */
flex-wrap: wrap;
flex-wrap: wrap-reverse;
```

### Values

- `nowrap`

  **Default value**. The flex items are laid out in a single line which may cause the flex container to overflow. The cross-start is either equivalent to start or before depending on the [`flex-direction`](./flex-direction.mdx) value.

- `wrap`

  The flex items break into multiple lines. The cross-start is either equivalent to start or before depending [`flex-direction`](./flex-direction.mdx) value and the cross-end is the opposite of the specified cross-start.

- `wrap-reverse`

  Behaves the same as `wrap` but cross-start and cross-end are permuted.

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>nowrap</>}
  appliesTo={<>flex containers</>}
  inherited="no"
/>

## Formal syntax

```
flex-wrap =
  nowrap        |
  wrap          |
  wrap-reverse
```

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/flex-wrap" />
