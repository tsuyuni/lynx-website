---
id: order
slug: order
title: order
---

## Introduction

The `order` CSS property sets the order to lay out an item in container. Items in a container are sorted by ascending order value and then by their source code order. Items not given an explicit order value are assigned the default value of `0`.

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/order/App.tsx"
  img="/assets/doc/css/order.jpg"
  entry="src/order"
  defaultEntryFile="dist/order.lynx.bundle"
/>

## Syntax

```css
/* Numerical value including negative numbers */
order: 5;
order: -5;
```

### Values

- [`<number>`](api/css/data-type/number.mdx)

Represents the ordinal group to be used by the item. Default value is `0`.

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0</>}
  appliesTo={
    <>
      child element with
      <a href="/api/css/properties/position.mdx">
        <code>position: relative</code>
      </a>
    </>
  }
  inherited="no"
  animatable="no"
/>

## Difference with the Web

1. It is not supported to use on [`position: absolute/fixed/sticky`](./position.mdx) flex and grid container children.
2. Using `order` will not influence the element's painting order in Lynx.

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/order" />
