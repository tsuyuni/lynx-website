---
id: flex-shrink
slug: flex-shrink
title: flex-shrink
---

## Introduction

Defines the shrink factor of the flex element on the main axis. After the flex container layout elements, if there is an element overflow, the corresponding element will be scaled according to the element's zoom factor ratio to ensure that the element will not overflow the container.

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/flex-shrink/App.tsx"
  img="/assets/doc/css/flex-shrink.jpg"
  entry="src/flex-shrink"
  defaultEntryFile="dist/flex-shrink.lynx.bundle"
/>

## Syntax

```css
/* <number> values */
flex-shrink: 2;
flex-shrink: 0;
```

### Values

- [`<number>`](api/css/data-type/number.mdx)

  Negative values are invalid. Defaults to `1`.

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>1</>}
  appliesTo={<>flex items</>}
  inherited="no"
/>

## Formal syntax

```
flex-shrink =
  <number [0,∞]>
```

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/flex-shrink" />
