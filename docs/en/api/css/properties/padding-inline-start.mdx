---
id: padding-inline-start
slug: padding-inline-start
title: padding-inline-start
---

## Introduction

Defines the logical inline start padding of an element, which maps to a physical padding depending on the element's [`direction`](./direction). Must be nonnegative.

In `LTR` mode, `padding-inline-start` taking the same values as the [`padding-left`](./padding-left).
In `RTL` mode, `padding-inline-start` taking the same values as the [`padding-right`](./padding-right).

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/padding-inline-start/App.tsx"
  img="/assets/doc/css/padding-inline.jpg"
  entry="src/padding-inline-start"
  defaultEntryFile="dist/padding-inline-start.lynx.bundle"
/>

## Syntax

```css
/* <length> values */
padding-inline-start: 0.5em;
padding-inline-start: 0px;
padding-inline-start: 2rpx;

/* <percentage> value */
padding-inline-start: 10%;
```

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0</>}
  appliesTo={<>all elements</>}
  inherited="no"
  percentages={<>refer to the width of the containing block</>}
/>

### Values

#### `0px`

**Default value**.

#### `<length>`

The size of the padding as a fixed value. Must be nonnegative.

#### `<percentage>`

The size of the padding as a percentage, relative to the inline size (width) of the [containing block](https://developer.mozilla.org/en-US/docs/Web/CSS/Containing_block).

## Formal syntax

```
padding-inline-start =
  <length-percentage [0,∞]>

<length-percentage> =
  <length>      |
  <percentage>
```

## Difference with the Web

1. `writing mode` is not supported in Lynx.

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/padding-inline-start" />
