---
id: height
slug: height
title: height
---

## Introduction

The `height` CSS property specifies the height of an element. By default, the property defines the height of the border area. If [`box-sizing`](./box-sizing.mdx) is set to `content-box`, however, it instead determines the height of the content area.

The [`min-height`](./min-height) and [`max-height`](./max-height) properties override `height`.

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/height/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/height.jpg"
  entry="src/height"
  defaultEntryFile="dist/height.lynx.bundle"
/>

## Syntax

```css
/* <length> values */
height: 300px;
height: 25em;

/* <percentage> value */
height: 75%;

/* Keyword values */
height: max-content;
height: fit-content;
height: fit-content(20em);
height: auto;
```

### Values

- `auto`

  **Default value**. lynx will calculate and select a height for the specified element.

- `<length>`

  [`<length>`](api/css/data-type/length.mdx) defines the height as a distance value.

- `<percentage>`

  Defines the height as a percentage of the [containing block](https://developer.mozilla.org/en-US/docs/Web/CSS/Containing_block)'s height.

- `max-content`

  The intrinsic preferred height.

- `fit-content`

  [`fit-content`](api/css/data-type/fit-content.mdx) uses the computed size with the available space replaced by the specified argument.

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      <code>auto</code>
    </>
  }
  appliesTo={<>all elements</>}
  inherited="no"
  animatable="yes"
  percentages={
    <>
      The percentage is calculated with respect to the height of the generated
      box's containing block
    </>
  }
/>

## Formal syntax

```
height =
  auto                                      |
  <length-percentage [0,∞]>                 |
  max-content                               |
  fit-content( <length-percentage [0,∞]> )

<length-percentage> =
  <length>      |
  <percentage>
```

## Difference with the web

- By default, the property defines the height of the border area, while Web is `content area`.
- `min-content` is not supported.

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/height" />
