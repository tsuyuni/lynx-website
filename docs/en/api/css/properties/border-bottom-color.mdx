# border-bottom-color

## Introduction

Sets the bottom border color of an element. Used when you need to specify the bottom border color separately.

## Usage Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/border-bottom-color/App.tsx"
  img="/assets/doc/css/border-color-edge.jpg"
  defaultEntryFile="dist/border-bottom-color.lynx.bundle"
  entry="src/border-bottom-color"
/>

## Syntax

```css
border-bottom-color: red;

border-bottom-color: '#ff0000';
```

### Values

- [`<color>`](/api/css/data-type/color.mdx)

## Formal Definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>Empty</>}
  appliesTo={<>All elements</>}
  inherited="no"
  animatable="yes"
/>

## Formal Syntax

```css
border-bottom-color = <color>
<color> = <rgb()> | <rgba()> | <hsl()> | <hsla()> | <hex-color> | <named-color>
```

## Differences from the Web

- [MDN Reference](https://developer.mozilla.org/en-US/docs/Web/CSS/border-bottom-color)
- Different default value (Web uses `currentColor`)
- Does not support global values like `inherit`, `initial`, `revert`, and `unset`

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/border-bottom-color" />
