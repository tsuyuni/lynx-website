---
id: linear-cross-gravity
slug: linear-cross-gravity
title: linear-cross-gravity
---

import { Deprecated } from '@lynx';

# <Deprecated /> linear-cross-gravity

:::warning
This API is deprecated.
:::

## Introduction

Set the position of the child element on the cross axis of the parent container in [linear layout](guide/ui/layout/linear-layout.mdx), is an style on the parent container. Similar to [`align-items`](./align-items.mdx) in [flexible box layout](guide/ui/layout/flexible-box-layout.mdx).

When using [`linear-layout-gravity`](./linear-layout-gravity.mdx), [`align-self`](./align-self.mdx), [`linear-cross-gravity`](./linear-cross-gravity.mdx), and [`align-items`](./align-items.mdx) simultaneously in a linear layout, the priority decreases from the first to the last. A higher-priority style will override a lower-priority style.

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/linear-cross-gravity/App.tsx"
  img="/assets/doc/css/linear-cross-gravity.png"
  defaultEntryFile="dist/linear-cross-gravity.lynx.bundle"
  entry="src/linear-cross-gravity"
/>

## Syntax

```css
linear-cross-gravity: none;
linear-cross-gravity: start;
linear-cross-gravity: end;
linear-cross-gravity: center;
linear-cross-gravity: stretch;
```

### Values

- `none`

  **Default value**. When the size of the parent element cross axis size(such as the [`width`](./width) when [`linear-direction: column`](./linear-direction)) is fixed, and the size of the child element in this direction is auto, `none` will be equivalent to `stretch`. In other cases it is equivalent to `start`.

- `start`

  Align the beginning of the parent element's cross axis.

- `end`

  Align the end of the parent element's cross axis.

- `center`

  Align the center of the parent element's cross axis.

- `stretch`

  Fill the container's cross axis.

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>none</>}
  appliesTo={<>linear containers</>}
  inherited="no"
/>

## Formal syntax

```
linear-cross-gravity = none | start | end | center | stretch
```

## Difference between web

- No such style in Web.

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/linear-cross-gravity" />
