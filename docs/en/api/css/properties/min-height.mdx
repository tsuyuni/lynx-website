---
id: min-height
slug: min-height
title: min-height
---

## Introduction

To specify min height of a view. It prevents the applied value of [`height`](./height) becoming larger than the specified value of `min-height`.
The priority of `min-height` is higher than [`height`](./height) but lower than [`max-height`](./max-height).

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/min-height/App.tsx"
  img="/assets/doc/css/min-height.jpg"
  entry="src/min-height"
  defaultEntryFile="dist/min-height.lynx.bundle"
/>

## Syntax

```css
/* <length> */
min-height: 120px;
min-height: 10em;

/* <percentage> */
min-height: 75%;
```

### Values

#### `<length>`

[`<length>`](api/css/data-type/length.mdx) defines the min-height as an absolute value. Default to `0px`.

#### `<percentage>`

Defines the `min-height` as a percentage of the containing block's height.

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0px</>}
  appliesTo={<>all elements</>}
  inherited="no"
  percentages={
    <>
      The percentage is calculated with respect to the height of the generated
      box's containing block
    </>
  }
  animatable="yes"
/>

## Formal syntax

```
min-height = <length-percentage [0,∞]>

<length-percentage> =
  <length>      |
  <percentage>
```

## Difference with the Web

[mdn:min-height](https://developer.mozilla.org/en-US/docs/Web/CSS/min-height)

1. In Lynx, the priority of `max-height` is higher than `height` and `min-height`. While in Web, `max-height` overrides `height`, but `min-height` overrides `max-height`.
2. If `min-height` is specified smaller than [`min-height`](./min-height.mdx), [`min-height`](./min-height.mdx) will be ignored.
3. `auto` is not supported, default value is `0px`.
4. `max-content`, `min-content`, `fit-content`, `min-content` are not supported.

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/min-height" />
