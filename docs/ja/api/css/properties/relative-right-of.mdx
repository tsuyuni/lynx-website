---
id: relative-right-of
slug: relative-right-of
title: relative-right-of
---

## Introduction

The current element is to the right of the sibling element specified corresponding to [`id`](./relative-id.mdx) in [relative-layout](guide/ui/layout/relative-layout.mdx).

## Examples

import { Go } from '@lynx';

<Go
  example="css-api"
  defaultFile="src/relative-right-of/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/relative-right-of.png"
  defaultEntryFile="dist/relative-right-of.lynx.bundle"
  entry="src/relative-right-of"
/>

## Values

### `none`

**Default Value**. Act as not set this property.

### `<number>`

`int` only. Specifies the sibling element number.(refer to [`relative-id`](./relative-id.mdx)).

:::info
Attention please when setting [`relative-id`](./relative-id.mdx): `-1` and `0` are special values. `-1` is the default value, indicating that the current element will not be considered as the alignment reference standard for sibling elements. Using `0` as an element ID is not allowed, as it will result in unexpected behavior.
:::

## Syntax

```css
relative-right-of: none;
relative-right-of: 1;
relative-right-of: 2;
```

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={
    <>
      <code>none</code>
    </>
  }
  appliesTo={<>relative items</>}
  inherited="no"
/>

## Formal syntax

```
relative-right-of = none | <number>
```

## Difference between web

- There is no such style in web.

## Compatibility

import { LegacyCompatTable } from '@lynx';

<LegacyCompatTable metadata="css/properties/relative-right-of" />
